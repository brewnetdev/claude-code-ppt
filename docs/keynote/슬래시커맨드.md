참석자 1 00:04
먼저 슬래시 커맨드입니다. 오프닝에서 이제 제가 단추 키라고 했죠.
컨트롤 c 컨트롤 v 이런 것들을 매번 마우스로만 조작하지는 않을 거예요.
이 단축키를 눌러서 이제 복사 붙여 놓기를 하게 되겠죠.
커맨드도 마찬가지입니다. 그래서 한번 외우면 반사적으로 쓸 수 있게 리뷰 커밋 그다음에 뭐 체크 포인트 이런 것들을 만들어서 쓰면 된다는 거죠.
그다음에 이 단추 키들은 내가 정의할 수 있어요. 점 클로드 밑에 커맨드라는 폴더 밑에 마크다운 하나를 만들어 두고 이게 이제 어떤 기능을 하는지 기술을 해두면 이게 하나의 슬래시 커멘드가 됩니다.
그래서 파트 1에서는 커스텀 커맨드 작성법 그다음에 아규먼트로 인자를 받아서 이 아규먼트를 활용하는 방법 실전 패턴들을 배우도록 하겠습니다.
네 커스텀 커맨드입니다. 점 클로드 밑에 커맨드라고 하는 폴더 밑에 반복되는 프롬프트를 파일로 박아두는 거예요.
그래서 슬래시 플러스 네임으로 호출을 하는 거고요.
yaml 그 프론트 매터를 사용을 합니다.

참석자 1 00:59
디스크립션 필드가 있어야 되고 기능을 수행하기 위해서 얼로우드 툴 배시를 쓸 건지 리드 라이트 그랩 이런 것들을 쓸 건지 이런 것들을 기술을 해 두면 됩니다.
첫 번째 프론트 멘턴은 뭐냐 하면 파일 상단에 블록이에요.
yaml이라고 하는 블록으로 이 영역들을 만들어 두는 거죠.
디스크립션과 얼로우 투스라고 하는 두 가지 영역이 있고요.
디스크립션에는 이 커맨드가 무엇을 하는지 정리를 합니다.
그다음에 얼로드 툴에는 이 커맨드가 쓸 수 있는 도구들을 기술을 합니다.
이렇게 해서 이제 뭐 깃이다 그러면 깃 명령하고 리드 그래 뭐 이런 것들을 추가해 놓을 수 있겠죠.
그다음에 이제 본문인데요. 프롬프트를 절차대로 입력한다라고 보시면 되겠습니다.
이렇게 단계별로 적어두면 모델이 그대로 따라옵니다.
스코프는 이제 점 클로드 밑에다가 커맨드 밑에 작성하면 프로젝트 전용이 되는 거고요.
팀 공유가 가능하겠죠.

참석자 1 01:50
유저 디렉터리에 점 클로드 밑에 있는 커맨드는 모든 프로젝트에서 쓰는 전역 커맨드입니다.
둘 다 있을 경우에는 프로젝트 쪽이 우선권이 있다라고 이전에 설명을 드렸습니다.
네 그다음에 이제 커맨드의 인자를 받는 건데요. 여기 위에 나온 것들이 이제 프론트 메터라고 말씀을 드렸잖아요.
이 프론트 메터라는 거는 yaml이라고 하는 양식으로 되어 있는 설명서에 가까운 읽기 전용 설명서에 가까운 사람이 읽기 쉬운 설정 파일 형식입니다.
디스크립션 그래서 이제 앞에 나오는 필드 키 값 제이슨하고 똑같죠.
키 밸류 형태의 앞에 나오는 키와 뒤에 나오는 밸류 이렇게 이제 이어져 있고요.
콜론 뒤에는 공백이 한 칸이 있어야 됩니다. 공백이 한 칸 있고 그 뒤에 동백이 한 칸 있고 그런 뒤에 공백이 한 칸 있고 들여쓰기는

참석자 1 03:01
그리고 들여쓰기는 탭이 아니라 스페이스 바로 들여쓰기를 해야 됩니다.
이런 원칙 간단한 원칙이 있는 양식이고요. 이걸 이제 야물이라고 하더라고요.
외국에서는 이걸 이제 외국 애들 발음으로는 야물이라고 발음을 하더라고요.
yaml이라고 합니다. 그다음에 이제 이 프론트 메터는 이 야물 블록을 얘기를 하는 거예요.
그래서 대시 3개로 표시되는 메시 3개로 표시되는 이 블록을 이제 이제 프론트 메터라고 하고요.
이게 이제 하나의 요 구분자 신분증 뭐 이런 거 이게 이제 하나의 구분자라고 보시면 되겠습니다.
네임이 있고 뭐 디스크립션이 있고 그래서 구조상으로는 상단에 프론트 메터 그다음에 본문의 바디 영역 이렇게 해서 2개의 영역으로 나눠져 있습니다.
그래서 본문은 지시문이고요. 무엇을 할지 기술하는 디스크립션 아 무엇을 할지 지시하는 프롬프트 형태가 되겠고요.
그다음에 프론트 메터는 언제 어떻게 실행을 할지 실행 조건에 대해서 권한과 함께 기술해 주는 겁니다.

참석자 1 04:18
스킬의 프론트 메터와 드 커맨드의 프론트 메터가 살짝 다른데요.

참석자 1 04:29
여기에는 이제 아규먼트 힌트가 나와 있는데 뭐 디세이블 모델 인보케이션 뭐 이런 것들이 있을 수 있고요.
디세이블 모델 인보케이션이라고 해서

여기에 이제 모델 컨텍스트 이런 것들을 추가할 수 있고요.
전체가 다 적용되어 있는 복잡한 예제들도

별도의 소스로 제공은 별도의 소스 예제로 보여드리려고 합니다.


그래서 첫 번째 거를 살펴보면요. 픽스 이슈라고 하는 커맨드인데 기트허브 넘버를 기트허브 이슈 번호를 잡 깃허브 이슈 번호를 받아가지고

참석자 1 05:37
먼저 왼쪽 예시 이슈 먼저 왼쪽에 먼저 왼쪽 예시 픽스 이슈를 보면요.
어 커멘드 부문 곳곳에 이런 아규먼트가 나옵니다.
여기에 아규먼트 이렇게 나오죠 여기도 나오고요.
예를 들어서 픽스 이슈 12 이렇게 치면 이 12라고 하는 숫자가 아규먼트 자리에 치환이 돼서 들어갑니다.
그래서 ghe슈 u 1e라고 하는 커맨드가 터미널 명령어가 실행이 되는 거죠.
그래서 여기에 12 여기에도 12 여기에도 12 이런 식으로 들어가서 이 하나의 프롬프트가 이 권한으로 이 이 권한으로 실행이 된다 이렇게 이해를 하시면 되겠습니다.
그래서 이거는 번호를 받아가지고 깃허브 이슈를

참석자 1 06:43
깃허브 이슈를 처리하는 거고요.

참석자 1 06:57
그 뒤에 PR 본문 그 뒤에 그 뒤에 PR 본문

참석자 1 07:10
그 뒤에 마지막으로는 피알 본문 초 피알 본문 초안을 작성한다 이렇게 이해하시면 되겠습니다.
두 번째는 어 에드 피처라고 하는 건데요. 폐 기능 스케폴딩입니다.
스케폴딩은 뭐냐 하면 껍데기들 디렉터리 구조를 만드는 거예요.
그래서 어 에드 피처라고 하는 커맨더로 들어왔을 때 커맨드 안에서 이제 정규식 검증을 해서 인자를 검증하고요.
소문자 하이푼 소문자 숫자 하이푼 이런 조합으로 정규식 검사를 한 뒤에 길이가 두 글자에서 40글자 사이인지 확인한 뒤에 이 뒤에 작업들을 진행을 하는 겁니다.

참석자 1 08:07
그래서 그래서 기존에 있는 패턴을 참고해서 mkdir 마이너스 p라고 하는 특정한 경로의 아규먼트로 받아온 거를 만드는 작업들을 하는 거고요.
컴포넌트 서브 액션 드리즐 그다음에 바이테스트 테스트 파일을 생성한다.
바이 테스트 테스트 파일을 바이 테스트 테스트 파일을 생성한다.
그다음에 생성 결과를 요약한다 이렇게 되어 있습니다.
이제 잘못된 인자가 들어왔을 때 모델이 무작정 이제 잘못된 인자 그대로 작정 모델이 이제 무작정 잘못된 인자를 받아가지고 작업하지 않도록 검증 절차를 커멘드 안에다가 이렇게 박아둔 겁니다.
그래서 일회성 지시가 아니라 재사용 가능한 지시 재사용 가능한 도구로 만드는 것 이런 것들을 인자를 받아서 이런 작업들을 진행을 한다 이렇게 이해하시면 되겠습니다.
이렇게 이해하시면 되겠습니다. 전 조합 실전 명령어 조합 패턴이에요.
TDD 사이클 우리 경험해 봤는데 TDD 레드 TDD 그린 TDD 리팩터 이렇게 돼 있잖아요.

참석자 1 09:14
그래서 각 단계별로 섞이지 못하게 분리해서 레드 그다음 그린 그다음 리팩터 이렇게 진행을 했습니다.
그런데 자동화된 파이프라인으로도 분리하지 않고 자동화된 파이프라인으로도 작성을 할 수가 있어요.
커미 전에 혐의전에 4단계의 점검 단계를 둬서 이제 이 4단계가 통과가 되면 자동으로 커밋이 되게끔 이 점검 단계를 자동화하는 그런 메시지 그런 파이프라인입니다.
이 점검 단계를 자동화하는 그런 파이프라인이라고 이런 그 그런 점검 단계 그런 점검 단계 그런 점검 단계를 그런 점검 단계를 자동화하는 파이프라인이라고 보시면 되는데요.
린트 픽스 한 뒤에 그다음에 체크 타입 타입 체크를 하고 그다음에

참석자 1 10:13
그다음에 테스트 에펙티드 변경에 영향받은 테스트 파일만 실행을 하고 그다음에 커밋 메시지 변경 디프 기반의 메시지를 생성한다.
그래서 이 4개를 순차적으로 각각 실행하는 게 아니라 클로드 밑에 커맨드 밑에 프리커미 MD 를 만들어 두고 이 본문에 이 네 단계를 다 기술을 해 두면 됩니다.
그러면 프리커밋의 훅을 걸 북을 걸어서 전체를 실행을 하게 되는 거죠.
그래서 실패한 단계에서는 멈추고 이제 수정을 하고 이어가는 그런 형태가 되겠습니다.
실패 실패 중간 단계가 실패를 하면 거기서 멈추고 고친 뒤에 이어 나가도록 이렇게 이제 이어지는

참석자 1 11:02
중간에 이제 실패가 나면 거기서 멈추고 고친 뒤에 이어 나갑니다.

참석자 1 11:12
그래서 이제 서버에 이제 돌리기 전에 빌드를 서버에 빌드를 돌리기 전에 로컬에서 이 순서대로 자동화된 테스트 파이프라인을 거쳐서 에러가 있는지 실패된 건이 있는지 이런 것들을 먼저 사전 점검한 뒤에 서버 쪽에 푸시를 하겠다 이럴 때 사용하는 겁니다.
그다음에 컨텍스트 오염 방지예요. 체크 포인트 만들고 클리어 한 뒤에 리스트 체크 포인트를 하면 이제 그 긴 세션 동안 쌓인 그 긴 세션 동안 쌓인 핵심 결정 사항들을 이제 상태별로 저장을 하고 클리어로 컨텍스트를 초기화한 뒤에 그다음에 리스트와 체크 포인트로 지정된 결정만 저장된 결정만 다시 로드를 하게 됩니다.
그러면 이제 누적 없이 깨끗한 컨텍스트

참석자 1 12:03
그러면 이제 누적 오류 없이 깨끗한 컨텍스트로 이제 재개가 되는 거죠.
커맨드는 한 작업 직업의 경계가 이제 커맨드는 한 작업 작업의 경계가 명확하게 끊어지는 것이 이제 끊어지게 만드는 것이 커멘드 설계의 핵심이고요.

참석자 1 12:28
가급적 작은 단위 하나의 책임을 갖는 체계로 커맨드를 설계를 하셔야 됩니다.
그다음은 이제 에이전트 스킬인데요. 다음 강의에서 에이전트 스킬에 대해서 에이전트 스킬에 대해서 다음 강의에서 설명을 하도록 하겠습니다.
감사합니다.